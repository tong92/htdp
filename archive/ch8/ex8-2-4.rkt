;; The first three lines of this file were inserted by DrRacket. They record metadata
;; about the language level of this file in a form that our tools can easily process.
#reader(lib "htdp-advanced-reader.ss" "lang")((modname ex8-2-4) (read-case-sensitive #t) (teachpacks ()) (htdp-settings #(#t constructor repeating-decimal #t #t none #f () #f)))
;; (define (f x) 'x)
;; collect, allways return 'x

;; (define (f 'x) x)
;; wrong, second parameter shoud var

;; (define (f x y) (+ 'y (not x)))
;; wrong, type error