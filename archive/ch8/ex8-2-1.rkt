;; The first three lines of this file were inserted by DrRacket. They record metadata
;; about the language level of this file in a form that our tools can easily process.
#reader(lib "htdp-advanced-reader.ss" "lang")((modname ex8-2-1) (read-case-sensitive #t) (teachpacks ()) (htdp-settings #(#t constructor repeating-decimal #t #t none #f () #f)))
;; x
;; exp

;; (= x z)
;; (prm exp exp)

;; (= (= y z) 0)
;; (prm exp cons)

;; (3 + 4)
;; exp's first parameter shoud var or prm

;; empty? (l)
;; exp's shoud wrapping parenthesis
;; one var is not wrapping parenthesis

;; (x)
;; one var is not wrapping parenthesis
